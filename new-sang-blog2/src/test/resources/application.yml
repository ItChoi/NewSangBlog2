spring:
  profiles:
    active: local

---

spring:
  profiles: local
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect

---

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG

---
